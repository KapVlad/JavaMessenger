<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_add_user"
            android:icon="@drawable/ic_menu_add"
            android:title="Add new contact" />
        <item
            android:id="@+id/nav_show_contacts"
            android:icon="@drawable/ic_menu_crop"
            android:title="Show contacts" />
        <item
            android:visible="false"
            android:id="@+id/nav_select_contact"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="Select contact" />
        <item
            android:id="@+id/nav_remove_contacts"
            android:icon="@drawable/ic_menu_close_clear_cancel"
            android:title="Remove contacts" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>
